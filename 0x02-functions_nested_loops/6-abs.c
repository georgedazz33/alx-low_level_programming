#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the int in question in this case
 * Return: the abs value of the int
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
